{
  "name": "cos30043-modern-webapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -k --kill-others-on-fail --names \"PHP,VUE\" --prefix-colors \"blue,green\" \"npm run dev:php\" \"npm run dev:vue\"",
    "dev:vue": "vite",
    "dev:php": "php -S localhost:8000 -t .",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.32"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.5",
    "concurrently": "^9.2.1",
    "vite": "^8.0.4"
  }
}